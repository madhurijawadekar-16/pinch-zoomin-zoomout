import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'pinch-zoom-js',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
